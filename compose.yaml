services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    depends_on:
      - backend

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    depends_on:
      - bot

  botTelegram:
    build:
      context: ./botTelegram
      dockerfile: Dockerfile
    ports:
      - "5001:5001"
